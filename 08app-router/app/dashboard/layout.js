const DashLayout = ({ children }) => {
    return (
        <div>
            <nav>
                <h1>NAVBAR</h1>
            </nav>
            {children}
        </div>
    );
};

export default DashLayout;